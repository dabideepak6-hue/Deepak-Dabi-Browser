# Deepak Dabi Browser

## Run
npm install
npm start

Note: Many websites block loading inside iframes. For a real browser with tabs and unrestricted rendering, the Electron app should use BrowserView/WebContents instead of an iframe.
